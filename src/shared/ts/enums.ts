enum EHLSEvents {
  HLS_ERROR = 'HLS ERROR',
  HLS_PROVIDER_ERROR = 'useHLSProvider must be used within an HLSProvider',
  HLS_BROWSER_NOT_SUPPOERTED = 'HLS is not supported on this browser',
}

export { EHLSEvents };
